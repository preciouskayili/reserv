# Enable Maps JavaScript API, Places API (New), and Geocoding API in this key's Google Cloud project.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# Backend API origin (use an HTTPS origin for production)
NEXT_PUBLIC_API_URL=http://localhost:4100
